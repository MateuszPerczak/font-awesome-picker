import type { FC } from "react";

import StyledFooter from "./Footer.styles";

const Footer: FC = (): JSX.Element => {
  return <StyledFooter>By Mateusz Perczak</StyledFooter>;
};

export default Footer;
